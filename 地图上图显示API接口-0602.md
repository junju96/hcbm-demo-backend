# 上图显示接口协议

## 更新 2026-06-02

- 新增 `POST /map/route/display/batch/add`，用于一次提交多条当前进程内临时路线；服务端保留已有临时路线，并按 `unique_id` 追加或覆盖同名路线。
- 新增 `POST /map/route/display/batch/delete` / `DELETE /map/route/display/batch/delete`，按 `unique_ids` 批量删除当前临时路线；删除后有剩余路线时广播更新后的 `map_route_display`，没有剩余路线时广播 `map_route_cleared`。
- 新增 `POST /map/object/batch/delete` / `DELETE /map/object/batch/delete`，按 `unique_ids` 批量删除正式地图对象，并返回成功与失败清单。

## 更新 2026-06-01

- 明确 `/map/route/display` 的路线保存语义：该接口会把路线保存为 `map_marker_service` 当前进程内的临时路线状态，前端可通过 `GET /map/route/display` 恢复显示；状态会保留到 `DELETE /map/route/display`、下一次 `POST /map/route/display` 覆盖或服务重启。
- `POST /map/route/display` 顶层 `color` 支持 RGB 颜色或 `type1`、`type2`、`type3`；三种 type 的颜色由地图后端 `config/service.ini` 的 `[route_display_color_types]` 配置。
- `/map/route/display` 仍不写入正式地图对象集合；需要重启后保留、参与 `/map/get/geometries` 或作为正式路线对象查询时，仍使用 `/map/route/add`。

## 更新 2026-05-24

- 手动态势 `POST /map/situation/add` 已支持 `range_points` / `range_geometry`，用于显示由多个点连成的不规则范围；没有不规则范围时继续兼容 `range_diameter` 圆形范围。
- Mission Control 地图工具栏新增“显示侦察范围”，前端通过系统设置中的 `vehicleRangeBaseURL` 请求 `GET /vehicle/range/all`，并把 `zcRangeCoords`、`djRangeCoords` 渲染为不规则多边形。
- 路径规划显示层改为半透明线条，并按 LineString 坐标顺序叠加方向箭头；系统设置 `routeDirectionAnimationEnabled` 控制方向箭头是否沿路线动态流动。
- 新增 `POST /map/route/display` / `GET /map/route/display` / `DELETE /map/route/display`，用于把任务规划后的路线结果保存为当前临时路线、恢复显示或清除；需要正式对象持久化时仍使用 `/map/route/add`。


## 1. 适用范围

本文定义 Mission Control 地图中所有“上图显示”能力的后端接口约束。

“上图显示”包括但不限于：

- 普通点位
- 多边形、圆形等正式几何要素
- 手动态势点
- 车辆点
- 融合态势点
- 车辆侦察范围
- 路网
- 禁行区 / 禁行点
- 路径规划结果

## 2. 开发原则

后续新增任何 `xx 显示` 功能，必须满足以下要求：

1. 必须有后端接口承载数据，不能只在前端临时绘制。
2. 持久对象至少要有一个“写入 / 更新接口”和一个“查询接口”；临时显示对象必须明确事件与刷新边界。
3. 如果需要实时刷新，必须补充事件接口，例如 SSE。
4. 前端允许存在绘制中的临时预览，但最终显示数据必须以后端返回结果为准。
5. 前端左侧绘图工具、右侧输入表单、外部业务模块，最终都必须落到同一套后端上图接口上，不能各自维护一份前端私有图层数据。

推荐模式：

- 写入 / 更新：`POST`
- 查询：`GET`
- 删除 / 清空：`DELETE`
- 实时刷新：`GET /events`

---

## 3. 地图基础要素接口

服务：`map_marker_service`

默认地址：`http://localhost:28001`

### 3.1 普通点与正式几何要素

#### 3.1.1 新增点位

- 方法：`POST`
- 路径：`/map/add/point`

请求示例：

```json
{
  "lat": 39.909231,
  "lng": 116.397128,
  "type": "default",
  "label": "测试点"
}
```

说明：

- 用于普通点位上图。
- 左侧地图工具栏绘制点、右侧手动输入点、外部接口上图，都应统一走此接口。

#### 3.1.2 新增多边形

- 方法：`POST`
- 路径：`/map/add/polygon`

请求示例：

```json
{
  "label": "测试多边形",
  "points": [
    { "lat": 39.909231, "lng": 116.397128, "alt": 0.0 },
    { "lat": 39.909800, "lng": 116.398100, "alt": 0.0 },
    { "lat": 39.908900, "lng": 116.398600, "alt": 0.0 }
  ]
}
```

说明：

- 用于正式几何图形上图，不是禁行区接口。
- 左侧多边形 / 矩形绘制默认应走该接口。

#### 3.1.3 新增圆形

- 方法：`POST`
- 路径：`/map/add/circle`

请求示例：

```json
{
  "lat": 39.909231,
  "lng": 116.397128,
  "alt": 0.0,
  "radius_m": 120.0,
  "label": "测试圆形"
}
```

说明：

- 用于正式圆形要素上图。
- 左侧圆形绘制、右侧手动输入圆形，统一走该接口。

#### 3.1.4 查询全部普通点与正式几何

- 方法：`GET`
- 路径：`/map/get/allstuff`

响应说明：

- 返回普通点和正式几何要素的聚合结果。
- 前端基础要素图层应以此接口为主。

#### 3.1.5 查询正式几何

- 方法：`GET`
- 路径：`/map/get/geometries`

说明：

- 仅返回正式几何要素，不含普通点。

#### 3.1.6 查询单个点位

- 方法：`GET`
- 路径：`/map/get/point/{point_index}`

#### 3.1.7 删除单个点位

- 方法：`DELETE`
- 路径：`/map/delete/point/{point_index}`

#### 3.1.8 删除单个正式几何

- 方法：`DELETE`
- 路径：`/map/delete/geometry/{geometry_index}`

#### 3.1.9 清空全部正式几何

- 方法：`DELETE`
- 路径：`/map/delete/geometries`

---

### 3.2 态势点与车辆点

#### 3.2.1 新增手动态势

- 方法：`POST`
- 路径：`/map/situation/add`

请求示例：

```json
{
  "lat": 39.9132,
  "lng": 116.4178,
  "label": "态势 A",
  "icon": "/rana_icon.png",
  "range_diameter": 600,
  "range_points": [
    { "lat": 39.9132, "lng": 116.4178, "alt": 43.5 },
    { "lat": 39.9150, "lng": 116.4230, "alt": 43.8 },
    { "lat": 39.9078, "lng": 116.4190, "alt": 43.2 }
  ]
}
```

- 说明：
  - 请求体仍可传 `icon`、`actions` 等态势展示字段。
  - `range_points` 至少需要 3 个点，前端优先按不规则多边形范围显示。
  - 同时传 `range_points` 和 `range_diameter` 时，`range_diameter` 只作为圆形范围兼容字段保留。
  - `POST /map/situation/add` 的响应已精简，不再回传 `feature.properties.icon` 和 `feature.properties.actions`。

#### 3.2.2 查询全部态势

- 方法：`GET`
- 路径：`/map/situation/get/all`

说明：

- 返回手动态势和车辆点的聚合结果。

#### 3.2.3 查询手动态势

- 方法：`GET`
- 路径：`/map/situation/get/manual`

#### 3.2.4 查询车辆点

- 方法：`GET`
- 路径：`/map/situation/get/vehicles`

#### 3.2.5 按名称查询态势

- 方法：`GET`
- 路径：`/map/situation/get/one`

查询参数示例：

```text
?name=态势 A
```

#### 3.2.6 更新车辆点

- 方法：`POST`
- 路径：`/map/vehicle/update`

请求示例：

```json
{
  "timestamp": "2026-03-17 14:25:30.000",
  "vehicle_id": "DC03",
  "VMF": 94075906,
  "longitude": 116.397128,
  "latitude": 39.909231,
  "altitude": 43.5,
  "heading": 180,
  "pitch": 5,
  "roll": -2,
  "speed": 15,
  "offset": 10
}
```

说明：

- 同一 `vehicle_id` 重复上报时按 upsert 更新位置与航向。
- 只有 `vehicle_id` 命中 `vehicle_base_data_service` 返回的 `id / vid`，或 `VMF` 命中 `vmfCode` 时才会上图。
- 车辆图标与单车基础信息目录绑定，按排序后的前四辆依次使用 `/cat1.svg` 到 `/cat4.svg`。

#### 3.2.7 查询车辆点聚合结果

- 方法：`GET`
- 路径：`/map/vehicle/get/all`

#### 3.2.8 删除手动态势

- 方法：`DELETE`
- 路径：`/map/situation/delete/{situation_index}`

#### 3.2.9 清空手动态势

- 方法：`DELETE`
- 路径：`/map/situation/clear`

#### 3.2.10 查询车辆侦察范围

- 服务默认地址：`http://localhost:28103`
- 方法：`GET`
- 路径：`/vehicle/range/all`

响应示例：

```json
[
  {
    "id": "HL01",
    "name": "HL01车",
    "type": 1,
    "vmfCode": "70916038",
    "zcRange": 3.0,
    "djRange": 15.0,
    "zcRangeCoords": [
      { "lon": 116.397128, "lat": 39.90925898, "elevation": 43.5 },
      { "lon": 116.39713412, "lat": 39.90925686, "elevation": 43.6 },
      { "lon": 116.39714005, "lat": 39.9092507, "elevation": 43.6 },
      { "lon": 116.397128, "lat": 39.90925898, "elevation": 43.5 }
    ],
    "djRangeCoords": [
      { "lon": 116.397128, "lat": 39.90936589, "elevation": 43.8 },
      { "lon": 116.39715862, "lat": 39.90935531, "elevation": 43.9 },
      { "lon": 116.39718825, "lat": 39.90932463, "elevation": 43.9 },
      { "lon": 116.397128, "lat": 39.90936589, "elevation": 43.8 }
    ]
  }
]
```

说明：

- `zcRangeCoords` 用于侦察范围，`djRangeCoords` 用于打击范围。
- 每组范围至少需要 3 个有效经纬度点；首尾闭合点可传可不传。
- 该接口由前端临时显示图层消费，不写入 `map_marker_service` 的正式地图对象集合。
- 如果需要把这些范围作为长期地图对象保存，应转换为 `/map/add/polygon` 或统一对象接口。

---

### 3.3 路网与禁行区

#### 3.3.1 新增路段

- 方法：`POST`
- 路径：`/map/network/add/edge`

请求示例：

```json
{
  "points": [
    { "lat": 39.909231, "lng": 116.397128 },
    { "lat": 39.909800, "lng": 116.398100 }
  ]
}
```

#### 3.3.2 查询全部路网与禁区

- 方法：`GET`
- 路径：`/map/network/get/all`

响应说明：

- 返回路网折线 `items`
- 返回禁区 `forbidden_items`

#### 3.3.3 替换整套路网

- 方法：`POST`
- 路径：`/map/network/replace`

说明：

- Geoman 编辑 / 橡皮擦删除路网后，前端应将当前剩余路网整体回写到该接口。

#### 3.3.4 清空路网

- 方法：`DELETE`
- 路径：`/map/network/clear`

说明：

- 当前业务里前端通常只做“隐藏路网”，不建议直接清空后端数据。

#### 3.3.5 保存路网

- 方法：`POST`
- 路径：`/map/network/save`

#### 3.3.6 加载路网

- 方法：`POST`
- 路径：`/map/network/load`

说明：

- 用于把后端路网数据重新加载到地图。

#### 3.3.7 查询禁区

- 方法：`GET`
- 路径：`/map/forbidden/get/all`

#### 3.3.8 设置全部禁区

- 方法：`POST`
- 路径：`/map/forbidden/set`

请求示例：

```json
{
  "forbidden": [
    {
      "forbidden_id": 1,
      "kind": "polygon",
      "data": [
        { "lat": 39.909231, "lng": 116.397128 },
        { "lat": 39.909800, "lng": 116.398100 },
        { "lat": 39.908900, "lng": 116.398600 }
      ]
    },
    {
      "forbidden_id": 2,
      "kind": "point",
      "data": { "lat": 39.910000, "lng": 116.399000 }
    }
  ]
}
```

说明：

- 左侧工具栏如果显式进入“绘制禁区”模式，完成绘制后应统一走该接口同步。
- 禁行区 / 禁行点删除后，也必须回写该接口。

---

### 3.4 路径规划显示

#### 3.4.1 路径规划

- 方法：`POST`
- 路径：`/map/route/plan`

请求示例：

```json
{
  "start_type": "point",
  "start_index": 1,
  "end_type": "point",
  "end_index": 8,
  "via_indices": [3, 5]
}
```

响应说明：

- 当前按请求中的 `requirement` 返回一条 `route`
- 同步返回 `summary`、`legs`、`targets`、`planned_points`
- 前端据此上图显示当前规划偏好的路线；路线按半透明线条显示，并按坐标顺序叠加方向箭头。`routeDirectionAnimationEnabled = true` 时方向箭头动态流动，关闭后保留静态箭头

#### 3.4.2 路线临时显示推送

- 方法：`POST`
- 路径：`/map/route/display`
- 批量添加：`POST /map/route/display/batch/add`
- 批量删除：`POST /map/route/display/batch/delete` 或 `DELETE /map/route/display/batch/delete`
- 配套查询：`GET /map/route/display`
- 配套清除：`DELETE /map/route/display`

请求可传：

- `route_result`：完整 `/map/route/plan` 响应，推荐任务规划链路使用
- `route`：GeoJSON LineString Feature
- `points`：`[{lat,lng,alt?}]` 点列
- `color`：可选，支持 RGB 颜色（`#RRGGBB`、`rgb(r,g,b)`）或 `type1`、`type2`、`type3`

说明：

- 后端保存为当前临时路线并广播 `map_route_display` 事件
- 前端收到事件后直接调用 `displayRouteResult()`，不会重新查询 `/map/get/geometries`
- 单条 `POST /map/route/display` 会覆盖当前临时路线；批量添加会保留已有临时路线，并按每条路线的 `unique_id` 追加或覆盖同名路线
- 批量删除请求体为 `{ "unique_ids": ["route-a", "route-b"] }`；删除后仍有路线时广播更新后的路线集合，没有剩余路线时广播 `map_route_cleared`
- `type1`、`type2`、`type3` 默认分别使用最短距离、宽路优先、坡度优先的原路线颜色；实际颜色可在地图后端 `config/service.ini` 的 `[route_display_color_types]` 中修改
- `color` 缺失、类型不匹配或 RGB 格式无效时，继续按原有路线颜色规则处理
- 当前临时路线会保存在 `map_marker_service` 进程内，可通过 `GET /map/route/display` 恢复；直到 `DELETE /map/route/display` 清除、下一次 `POST /map/route/display` 覆盖或服务重启后才消失
- `DELETE /map/route/display` 广播 `map_route_cleared`，前端收到后清空临时路线图层
- 该接口维护的是进程内临时路线状态，不写入正式地图对象集合；如需重启后保留、参与 `/map/get/geometries` 或作为正式路线对象查询，使用 `/map/route/add`

#### 3.4.3 地图事件流

- 方法：`GET`
- 路径：`/map/events`

说明：

- SSE 事件流
- 地图基础要素、态势、车辆、路网等变化后可通过该接口触发前端刷新
- `map_route_display` 事件用于触发前端临时路线图层绘制，`map_route_cleared` 用于清空临时路线图层

---

## 4. 融合态势接口

服务：`fusion_situation_service`

默认地址：`http://localhost:28006`

### 4.1 查询融合态势

- 方法：`GET`
- 路径：`/fusion-situations`

响应说明：

- 返回 `items`
- 返回 `count`
- 返回 `updated_at`

### 4.2 接收融合态势并更新显示

- 方法：`POST`
- 路径：`/fusion-situations/ingest`

请求示例：

```json
{
  "timestamp": "2026-03-16 18:02:34",
  "target_count": 2,
  "fused_targets": [
    {
      "time": "2026-03-10 10:00:02",
      "fusion_id": "F-faa958c0",
      "target_type": "238",
      "lon": 116.01003,
      "lat": 39.01,
      "alt": 11.2,
      "target_attr": 1
    }
  ]
}
```

说明：

- 外部融合结果写入后，前端通过查询接口或事件接口刷新显示。
- 推荐用 `fusion_id` 作为融合目标的稳定唯一值，并提供 `GET /fusion-situations/focus/{fusion_id}` 供地图直接定位。

### 4.3 融合态势事件流

- 方法：`GET`
- 路径：`/fusion-situations/events`

说明：

- SSE 事件流
- 接收到新的融合态势后，服务端推送刷新事件，前端再重新查询 `/fusion-situations`

---

## 5. 前端接入要求

前端实现地图显示时，必须遵守以下规则：

1. 左侧绘图工具完成后，必须调用后端接口入库，再由查询接口回显。
2. 右侧输入框手动上图，必须调用后端接口，不能只在浏览器内生成图层。
3. 实时消息流只负责触发刷新，不应替代查询接口本身。
4. 删除、编辑、清空操作必须同步更新后端，不能只删前端图层。
5. 同一类显示对象必须复用同一套后端接口，不允许再引入前端私有格式。
6. 地图局部操作反馈默认在地图操作模块内显示，不默认进入全局通知中心。
路径规划结果可作为例外同步进入通知中心。

## 6. 后续扩展模板

后续若新增 `无人机显示`、`雷达探测显示`、`告警区域显示` 等功能，至少应补齐：

1. `POST /xxx/add` 或 `POST /xxx/update`
2. `GET /xxx/get/all`
3. `DELETE /xxx/delete/{id}` 或 `POST /xxx/set`
4. 如需实时刷新，再补 `GET /xxx/events`

不允许仅在前端本地绘制后直接当作正式显示结果使用。
