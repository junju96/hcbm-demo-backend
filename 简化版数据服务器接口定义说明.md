# applications0.3 三池过滤查询与 simple/pick API 使用指引

> 文档版本：Pool Query & Projection Consumer Guide v0.3
> 适用分支：`data_pool`
> 最后更新：2026-06-18
> 文档状态：draft
> 目标读者：`task_pool`、`resource_pool_refactor`、`situation_pool` 的外部调用方、联调方、前端或其它需要从池中查询数据的服务

## 1. 文档目的

本文档面向池的调用方，不讨论池内部代码实现。

调用方只需要知道下面这些信息：

- 应该访问哪个服务、哪个端口。
- `task_pool` 如何按条件过滤查询任务、按关键字搜索任务。
- `task_pool`、`resource_pool_refactor`、`situation_pool` 如何获取轻量的默认精选视图（`simple`）。
- `task_pool`、`resource_pool_refactor`、`situation_pool` 如何按自定义字段裁剪返回结果（`pick`）。
- 三个池在过滤维度、字段路径、默认模板和筛选方式上的差异分别是什么。

## 2. 一句话结论

如果你想从 `task_pool` 查询数据：

- 多条件精确过滤用 `POST /api/v1/task_pool/resources/query`
- 关键字检索用 `POST /api/v1/task_pool/resources/search`
- 默认轻量视图用 `GET /api/v1/task_pool/resources/simple` 系列
- 显式字段投影（include/exclude）用 `POST /api/v1/task_pool/resources/pick/{task_type}`

其中 `query` 和 `search` 默认返回轻量精简结果。如果需要完整对象加 `total`，可以使用 `/api/v1/task_pool/retrieval/query` 或 `/api/v1/task_pool/retrieval/search`。

如果你想从 `resource_pool_refactor` 查询字段投影数据：

- 默认轻量视图用 `GET /api/v1/resource_pool/resources/simple` 系列
- 显式字段投影用 `POST /api/v1/resource_pool/resources/pick`
- `resource_pool_refactor` 的 `pick` 不走路径参数 `/{task_type}`，而是通过查询参数 `entity_kind` 做可选筛选

如果你想从 `situation_pool` 查询字段投影数据：

- 默认轻量视图用 `GET /api/v1/situation_pool/resources/simple` 系列
- 显式字段投影用 `POST /api/v1/situation_pool/resources/pick`
- 筛选使用 `situation_type`，例如 `OBSTACLE`、`OPTICAL_TARGET`、`FUSION_TARGET` 等

## 3. 服务地址与端口

| 服务 | 默认端口 | 说明 |
| --- | --- | --- |
| `task_pool` | `28801` | 任务池业务服务 |
| `resource_pool_refactor` | `28800` | 资源池重构版业务服务 |
| `situation_pool` | `28802` | 态势池业务服务 |

对调用方来说，应优先记住下面这条：

- 任务池统一入口：`http://<host>:28801`
- 资源池统一入口：`http://<host>:28800`
- 态势池统一入口：`http://<host>:28802`

如果部署环境和默认端口不一致，应以实际部署地址为准。

## 4. 对外 API 清单

以下接口基于：

- `http://<host>:28801`
- `http://<host>:28800`
- `http://<host>:28802`

### 4.1 过滤查询接口

| 方法 | 路径 | 请求体 | 返回类型 | 主要用途 |
| --- | --- | --- | --- | --- |
| `POST` | `/api/v1/task_pool/resources/query` | `TaskQueryRequest` | `list[dict[str, Any]]` | 多条件过滤，返回默认精简视图 |
| `POST` | `/api/v1/task_pool/resources/search` | `TaskSearchRequest` | `list[dict[str, Any]]` | 关键字检索，返回默认精简视图 |
| `POST` | `/api/v1/task_pool/retrieval/query` | `TaskQueryRequest` | 含 `items` + `total` | 多条件过滤，返回完整列表响应 |
| `POST` | `/api/v1/task_pool/retrieval/search` | `TaskSearchRequest` | 含 `items` + `total` | 关键字检索，返回完整列表响应 |

说明：

- `/resources/query` 和 `/resources/search` 默认返回轻量字段集合。
- `/retrieval/query` 和 `/retrieval/search` 返回完整对象列表加 `total`。
- 两组接口语义不同，按需选择。

### 4.2 task_pool simple 系列接口

以下接口统一基于前缀 `/api/v1/task_pool/resources`，返回默认或自定义精简视图。

| 方法 | 路径 | 请求体 | 返回 | 用途 |
| --- | --- | --- | --- | --- |
| `GET` | `/simple` | 无 | `list[dict]` | 全量列表默认精选视图 |
| `POST` | `/simple/query` | `TaskSimpleRequest` | `list[dict]` | 全量列表自定义 simple 投影 |
| `GET` | `/simple/by_type/{task_type}` | 无 | `list[dict]` | 按类型默认精选视图 |
| `POST` | `/simple/by_type/{task_type}` | `TaskSimpleRequest` | `list[dict]` | 按类型自定义 simple 投影 |
| `GET` | `/simple/get_latest_by_type/{task_type}` | 无 | `dict` | 按类型最新一条的默认视图 |
| `POST` | `/simple/get_latest_by_type/{task_type}` | `TaskSimpleRequest` | `dict` | 按类型最新一条的自定义视图 |
| `GET` | `/simple/{resource_id}` | 无 | `dict` | 单资源默认精选视图 |
| `POST` | `/simple/{resource_id}` | `TaskSimpleRequest` | `dict` | 单资源自定义 simple 投影 |

说明：

- `simple` 只做白名单投影（include 风格）。
- 不传 `paths` 时，使用服务端预设的默认字段模板。
- 传了 `paths` 或 `fields` 时，按自定义路径裁剪。

### 4.3 task_pool pick 接口

基于前缀 `/api/v1/task_pool/resources`：

| 方法 | 路径 | 请求体 | 返回 | 用途 |
| --- | --- | --- | --- | --- |
| `POST` | `/pick/{task_type}` | `TaskPickRequest` | `list[dict]` | 按类型显式 include/exclude 裁剪字段 |

说明：

- `pick` 支持 `include`（白名单）和 `exclude`（黑名单）两种模式。
- `pick` 只按 `task_type` 筛选，不做多维过滤。

### 4.4 resource_pool_refactor 的 simple 系列接口

以下接口统一基于前缀 `/api/v1/resource_pool/resources`，返回默认或自定义精简视图。

| 方法 | 路径 | 请求体 | 返回 | 用途 |
| --- | --- | --- | --- | --- |
| `GET` | `/simple` | 无 | `list[dict]` | 全量列表默认精选视图 |
| `POST` | `/simple/query` | `ResourceSimpleRequest` | `list[dict]` | 全量列表自定义 simple 投影 |
| `GET` | `/simple/by_type/{entity_kind}` | 无 | `list[dict]` | 按资源类型默认精选视图 |
| `POST` | `/simple/by_type/{entity_kind}` | `ResourceSimpleRequest` | `list[dict]` | 按资源类型自定义 simple 投影 |
| `GET` | `/simple/get_latest_by_type/{entity_kind}` | 无 | `dict` | 按资源类型最新一条的默认视图 |
| `POST` | `/simple/get_latest_by_type/{entity_kind}` | `ResourceSimpleRequest` | `dict` | 按资源类型最新一条的自定义视图 |
| `GET` | `/simple/{resource_id}` | 无 | `dict` | 单资源默认精选视图 |
| `POST` | `/simple/{resource_id}` | `ResourceSimpleRequest` | `dict` | 单资源自定义 simple 投影 |

说明：

- `resource_pool_refactor` 的 `simple` 也是白名单投影。
- 不传 `paths` 时，服务端统一使用设备默认白名单模板。
- `by_type` 路径里虽然叫 `by_type`，实际筛选字段是 `entity_kind`，例如 `equipment`、`platform`、`payload`、`component`、`ammo`、`sensor`、`supply`。

### 4.5 resource_pool_refactor 的 pick 接口

基于前缀 `/api/v1/resource_pool/resources`：

| 方法 | 路径 | 请求体 | 额外查询参数 | 返回 | 用途 |
| --- | --- | --- | --- | --- | --- |
| `POST` | `/pick` | `ResourcePickRequest` | `entity_kind`、`include_deleted`、`limit` | `list[dict]` | 全量或按资源类型做 include/exclude 裁剪 |

说明：

- `pick` 支持 `include`（白名单）和 `exclude`（黑名单）两种模式。
- 不传 `entity_kind` 时，对全量资源列表投影。
- 传 `entity_kind` 时，会先按资源类型过滤，再执行字段投影。

### 4.6 situation_pool 的 simple 系列接口

以下接口统一基于前缀 `/api/v1/situation_pool/resources`，返回默认或自定义精简视图。

| 方法 | 路径 | 请求体 | 返回 | 用途 |
| --- | --- | --- | --- | --- |
| `GET` | `/simple` | 无 | `list[dict]` | 全量列表默认精选视图 |
| `POST` | `/simple/query` | `SituationSimpleRequest` | `list[dict]` | 全量列表自定义 simple 投影 |
| `GET` | `/simple/by_type/{situation_type}` | 无 | `list[dict]` | 按态势类型默认精选视图 |
| `POST` | `/simple/by_type/{situation_type}` | `SituationSimpleRequest` | `list[dict]` | 按态势类型自定义 simple 投影 |
| `GET` | `/simple/{resource_id}` | 无 | `dict` | 单资源默认精选视图 |
| `POST` | `/simple/{resource_id}` | `SituationSimpleRequest` | `dict` | 单资源自定义 simple 投影 |

说明：

- `situation_pool` 的 `simple` 也是白名单投影。
- 不传 `paths` 时，服务端统一使用态势默认白名单模板。
- `by_type` 中的 `situation_type` 与 `task_pool` 概念不同，典型的 `situation_type` 包括：`OBSTACLE`、`FUSION_TARGET`、`OPTICAL_TARGET`、`FIXED_TARGET`、`MOVABLE_TARGET`、`DC_TARGET`、`TASK_TARGET`、`DW_COMPARISON` 等。
- `situation_pool` 的 `simple` 没有 `get_latest_by_type` 路由。

### 4.7 situation_pool 的 pick 接口

基于前缀 `/api/v1/situation_pool/resources`：

| 方法 | 路径 | 请求体 | 额外查询参数 | 返回 | 用途 |
| --- | --- | --- | --- | --- | --- |
| `POST` | `/pick` | `SituationPickRequest` | `situation_type`、`include_deleted`、`limit` | `list[dict]` | 全量或按态势类型做 include/exclude 裁剪 |

说明：

- `pick` 支持 `include`（白名单）和 `exclude`（黑名单）两种模式。
- 不传 `situation_type` 时，对全量态势列表投影。
- 传 `situation_type` 时，会先按态势类型过滤，再执行字段投影。

## 5. 请求参数说明

### 5.1 `TaskQueryRequest`

用于 `POST /query` 接口。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `task_type` | `string \| null` | 按任务类型过滤，如 `PLAN`、`STAGE`、`TEAM` |
| `state` | `string \| null` | 按状态精确过滤（大小写不敏感） |
| `relation_target` | `string \| null` | 按 `relations[].target` 精确过滤 |
| `source_topic` | `string \| null` | 按 `source.source_topic` 精确过滤 |
| `keyword` | `string \| null` | 对标题、描述等文本字段做包含匹配 |
| `parent_resource_id` | `string \| null` | 按父级资源 ID 过滤（匹配 `plan_resource_id`、`stage_resource_id`、`team_resource_id`、`command_resource_id`） |
| `include_deleted` | `bool` | 是否包含 `DELETED` 状态数据，默认 `false` |
| `mission_ids` | `list[string] \| null` | 结果必须同时包含所有这些 mission ID |
| `limit` | `int` | 返回条数上限，默认 `50`，范围 `1-500` |

### 5.2 `TaskSearchRequest`

用于 `POST /search` 接口。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `query` | `string` | 关键字字符串，按空白分词，必填 |
| `limit` | `int` | 返回条数上限，默认 `10`，范围 `1-100` |

### 5.3 `TaskSimpleRequest`

用于 `task_pool` 的 `POST /simple/...` 系列接口。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `paths` | `list[string] \| null` | 自定义字段路径，为空时使用默认模板 |
| `fields` | `list[string] \| null` | 兼容旧字段名，最终会归一到 `paths` |

### 5.4 `TaskPickRequest`

用于 `task_pool` 的 `POST /pick/{task_type}` 接口。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `mode` | `"include"` 或 `"exclude"` | 白名单或黑名单模式 |
| `paths` | `list[string] \| null` | 字段路径列表（include 模式至少需传 `paths` 或 `template`；exclude 模式必须传 `paths`） |
| `template` | `dict \| null` | 旧版 include 模板，兼容输入 |
| `fields` | `list[string] \| null` | 旧版字段名，兼容输入，会自动归一到 `paths` |

### 5.5 `ResourceSimpleRequest`

用于 `resource_pool_refactor` 的 `POST /simple/...` 系列接口。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `paths` | `list[string] \| null` | 自定义字段路径，为空时使用设备默认白名单模板 |
| `fields` | `list[string] \| null` | 兼容旧字段名，最终会归一到 `paths` |

### 5.6 `ResourcePickRequest`

用于 `resource_pool_refactor` 的 `POST /pick` 接口。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `mode` | `"include"` 或 `"exclude"` | 白名单或黑名单模式 |
| `paths` | `list[string] \| null` | 字段路径列表；include 模式至少需传 `paths` 或 `template`，exclude 模式必须传 `paths` |
| `template` | `dict \| null` | 旧版 include 模板，兼容输入 |
| `fields` | `list[string] \| null` | 旧版字段名，兼容输入，会自动归一到 `paths` |

### 5.7 `resource_pool_refactor` 的 `/pick` 查询参数

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `entity_kind` | `string \| null` | 可选，按资源类型筛选，例如 `equipment`、`platform`、`payload` |
| `include_deleted` | `bool` | 是否包含删除态资源，默认 `false` |
| `limit` | `int` | 返回条数上限，默认 `100` |

### 5.8 `SituationSimpleRequest`

用于 `situation_pool` 的 `POST /simple/...` 系列接口。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `paths` | `list[string] \| null` | 自定义字段路径，为空时使用态势默认白名单模板 |
| `fields` | `list[string] \| null` | 兼容旧字段名，最终会归一到 `paths` |

### 5.9 `SituationPickRequest`

用于 `situation_pool` 的 `POST /pick` 接口。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `mode` | `"include"` 或 `"exclude"` | 白名单或黑名单模式 |
| `paths` | `list[string] \| null` | 字段路径列表；include 模式至少需传 `paths` 或 `template`，exclude 模式必须传 `paths` |
| `template` | `dict \| null` | 旧版 include 模板，兼容输入 |
| `fields` | `list[string] \| null` | 旧版字段名，兼容输入，会自动归一到 `paths` |

### 5.10 `situation_pool` 的 `/pick` 查询参数

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `situation_type` | `string \| null` | 可选，按态势类型筛选，例如 `OBSTACLE`、`OPTICAL_TARGET`、`FUSION_TARGET` |
| `include_deleted` | `bool` | 是否包含删除态，默认 `false` |
| `limit` | `int` | 返回条数上限，默认 `100` |

## 6. 过滤语义

### 6.1 `POST /query` 的过滤规则

- 不同过滤维度之间是 **AND**。
- `include_deleted=false` 时，`state=DELETED` 的数据会被排除。
- `task_type` 和 `state` 采用大小写不敏感的精确匹配。
- `source_topic` 对应对象内 `source.source_topic`，精确匹配。
- `parent_resource_id` 匹配 `plan_resource_id`、`stage_resource_id`、`team_resource_id`、`command_resource_id` 中的任意一个。
- `relation_target` 匹配 `relations[].target`。
- `keyword` 采用小写包含匹配，会扫描标题、描述、`search_text`、`attributes`、`target_name` 等多个文本字段。
- `mission_ids` 采用**全部包含**语义（请求中的每个 mission ID 都必须出现）。
- 结果按 `updated_at` 倒序排列，再按 `limit` 截断。

### 6.2 `POST /search` 的检索规则

- `query` 按空白拆成多个 term。
- 每条数据按多字段命中情况累计分数：标题和名称命中加分最高，其次是 `task_type`，再其次是描述、目标、状态等字段。
- 最终按 `(score, updated_at)` 倒序排序，返回前 `limit` 条。
- `search` 固定查询非删除数据。

注意：`search` 是分词匹配加打分排序，不是结构化过滤，不能替代 `query` 做精确条件查询。

## 7. 字段路径规则

三个池的 `simple` / `pick` 都使用字段路径来指定需要保留或排除的字段。

规则如下：

- 使用点路径，例如 `stages.stage_id`、`tactic.suggest_teams.name`、`payloads.components.ammos.ammunition`。
- 不支持数组通配符。
- 不支持数组下标。
- 空路径会被忽略。
- 非法路径会返回 `400`。

### 路径示例

```json
// simple 请求：只返回这些字段
{
  "paths": ["resource_id", "plan_id", "plan_title", "state"]
}

// pick 请求：白名单模式，含嵌套字段
{
  "mode": "include",
  "paths": [
    "resource_id",
    "plan_id",
    "stages.stage_id",
    "stages.stage_title"
  ]
}

// pick 请求：黑名单模式，排除大字段
{
  "mode": "exclude",
  "paths": ["raw_payload", "search_text", "relations"]
}
```

## 8. `simple` 和 `pick` 的区别

| 维度 | `simple` | `pick` |
| --- | --- | --- |
| 定位 | 默认精选视图 | 显式字段投影 |
| 是否支持默认模板 | 支持 | 不自动兜底，必须显式传参 |
| 是否支持 exclude | 不支持（仅白名单） | 支持 include 和 exclude |
| 是否要求传类型筛选 | 不总是 | `task_pool` 必须通过路径传 `task_type`；`resource_pool_refactor` 可选通过查询参数传 `entity_kind`；`situation_pool` 可选通过查询参数传 `situation_type` |
| 典型场景 | 列表页、概览卡片、轻量展示 | 算法按需取字段、定制投影 |

## 9. 默认 simple 模板

### 9.1 task_pool 默认模板

`task_pool` 的 `simple` 系列接口在不传 `paths` 时，会按对象的 `task_type` 自动套用预设模板。

当前已为以下 `task_type` 定义了默认模板：

- `MISSION`、`COMMAND`、`PLAN`、`TACTIC`
- `TEAM`、`ACTION`、`CAR_ACTIONS`、`STAGE`

其中 `PLAN` 和 `STAGE` 的默认模板覆盖嵌套层级最深，适合算法对接。`TEAM` 和 `ACTION` 的模板相对精简，适合列表展示。

注意：如果某个 `task_type` 没有默认模板，当前实现会跳过该对象不返回。

### 9.2 resource_pool_refactor 默认模板

`resource_pool_refactor` 的 `simple` 系列接口在不传 `paths` 时，统一使用设备默认白名单模板，而不是像 `task_pool` 一样按不同类型切换模板。

当前默认模板覆盖的重点字段包括：

- 设备顶层字段：`resource_id`、`resource_name`、`entity_kind`、`model_type`
- 状态字段：`lifecycle_status`、`online_status`、`equipment_reports`、`deviation_alert`
- 平台字段：`platforms.platform_type`、`platforms.motion_status`、`platforms.health_status`
- 载荷与组件字段：`payloads.components.capacity`、`payloads.components.description`
- 弹药与传感器字段：`payloads.components.ammos.ammunition`、`payloads.components.sensors.capacity`

注意：

- 该默认模板明显偏向 `equipment` 资源视角。
- 即使调用 `/simple/by_type/platform` 或 `/simple/by_type/payload`，默认模板仍然是这套设备白名单，只是最终返回结果中仅保留当前资源对象能匹配到的字段。

### 9.3 situation_pool 默认模板

`situation_pool` 的 `simple` 系列接口在不传 `paths` 时，统一使用态势默认白名单模板。该模板按目标类型拆成了多组白名单，覆盖较广：

- **基础公共字段**：`resource_id`、`situation_type`、`attributes`、`search_text`、`source`、`raw_payload`、`relations`、`created_at`、`updated_at`、`state`、`connections`、`dependencies`
- **固定目标 FIXED_TARGET**：`object_id`、`name`、`foe`、`center_position`、`front`、`depth`、`area`
- **移动目标 MOVABLE_TARGET**：`object_id`、`name`、`foe`、`center_position`、`speed`、`heading`、`object_status`
- **障碍物 OBSTACLE**：`vid`、`obstacle_id`、`obstacle_type`、`location`、`impact_level`
- **光电目标 OPTICAL_TARGET**：`target_id`、`target_name`、`target_attr`、`target_mark`、`target_motion`、`image_url`
- **融合目标 FUSION_TARGET**：`fusion_id`、`target_type`、`location`、`threat`
- **DC 目标 DC_TARGET**：`vid`、`target_id`、`target_type`、`emitter_type`、`foe`、`threat_level`
- **战况对比 DW_COMPARISON**：`timestamp`、`counts`、`friendly`、`enemy`
- **任务目标 TASK_TARGET**：`plan_id`、`target_name`、`target_location`、`target_motion`、`situation_state`

注意：

- 该模板是一次性包含所有目标类型字段的大集合，不同于 `task_pool` 按 `task_type` 分别定义模板。
- 最终返回结果中只会保留当前数据对象中实际存在的字段。

## 10. 调用示例

### task_pool

#### 示例 1：多条件过滤查询

```http
POST /api/v1/task_pool/resources/query
Content-Type: application/json

{
  "task_type": "PLAN",
  "keyword": "巡控",
  "limit": 20
}
```

#### 示例 2：关键字检索

```http
POST /api/v1/task_pool/resources/search
Content-Type: application/json

{
  "query": "PLAN 区域巡控",
  "limit": 10
}
```

#### 示例 3：获取 PLANS 的默认精选视图

```http
GET /api/v1/task_pool/resources/simple/by_type/PLAN
```

#### 示例 4：自定义字段的 simple 投影

```http
POST /api/v1/task_pool/resources/simple/by_type/PLAN
Content-Type: application/json

{
  "paths": ["resource_id", "plan_id", "plan_title", "state", "stages.stage_id", "stages.stage_title"]
}
```

#### 示例 5：pick 白名单模式

```http
POST /api/v1/task_pool/resources/pick/PLAN
Content-Type: application/json

{
  "mode": "include",
  "paths": ["resource_id", "plan_id", "plan_title", "state", "stages.stage_id", "stages.state"]
}
```

#### 示例 6：pick 黑名单模式

```http
POST /api/v1/task_pool/resources/pick/PLAN
Content-Type: application/json

{
  "mode": "exclude",
  "paths": ["raw_payload", "search_text", "relations"]
}
```

### resource_pool_refactor

#### 示例 7：获取默认 simple 列表

```http
GET /api/v1/resource_pool/resources/simple
```

#### 示例 8：按 entity_kind 获取默认 simple 视图

```http
GET /api/v1/resource_pool/resources/simple/by_type/equipment
```

#### 示例 9：自定义字段的 simple 投影

```http
POST /api/v1/resource_pool/resources/simple/by_type/equipment
Content-Type: application/json

{
  "paths": [
    "resource_id",
    "resource_name",
    "entity_kind",
    "online_status",
    "payloads.components.resource_id",
    "payloads.components.ammos.ammunition"
  ]
}
```

#### 示例 10：pick 白名单模式

```http
POST /api/v1/resource_pool/resources/pick?entity_kind=equipment&limit=20
Content-Type: application/json

{
  "mode": "include",
  "paths": [
    "resource_id",
    "resource_name",
    "entity_kind",
    "lifecycle_status",
    "online_status",
    "payloads.components.resource_id"
  ]
}
```

#### 示例 11：pick 黑名单模式

```http
POST /api/v1/resource_pool/resources/pick?entity_kind=equipment
Content-Type: application/json

{
  "mode": "exclude",
  "paths": ["raw_payload", "search_text", "relations", "source"]
}
```

### situation_pool

#### 示例 12：获取默认 simple 列表

```http
GET /api/v1/situation_pool/resources/simple
```

#### 示例 13：按 situation_type 获取默认 simple 视图

```http
GET /api/v1/situation_pool/resources/simple/by_type/OBSTACLE
```

#### 示例 14：自定义字段的 simple 投影

```http
POST /api/v1/situation_pool/resources/simple/by_type/OPTICAL_TARGET
Content-Type: application/json

{
  "paths": [
    "resource_id",
    "situation_type",
    "state",
    "target_id",
    "target_name",
    "target_mark",
    "target_motion",
    "image_url"
  ]
}
```

#### 示例 15：pick 白名单模式

```http
POST /api/v1/situation_pool/resources/pick?situation_type=FUSION_TARGET&limit=20
Content-Type: application/json

{
  "mode": "include",
  "paths": [
    "resource_id",
    "fusion_id",
    "target_type",
    "target_attr",
    "location",
    "threat",
    "state"
  ]
}
```

#### 示例 16：pick 黑名单模式

```http
POST /api/v1/situation_pool/resources/pick?situation_type=OBSTACLE
Content-Type: application/json

{
  "mode": "exclude",
  "paths": ["raw_payload", "search_text", "relations", "dependencies"]
}
```

## 11. 推荐接入方式

- 列表页、概览页：优先使用 `GET /simple` 或 `GET /simple/by_type/{task_type}`
- 多条件精确过滤：用 `POST /resources/query`
- 关键字模糊搜索：用 `POST /resources/search`
- 需要完整列表加 `total`：用 `POST /retrieval/query` 或 `POST /retrieval/search`
- 自定义字段裁剪：用 `POST /simple/...`（白名单）或 `POST /pick/{task_type}`（白名单/黑名单）
- `resource_pool_refactor` 做轻量展示：优先用 `GET /api/v1/resource_pool/resources/simple`
- `resource_pool_refactor` 做按类型投影：用 `GET/POST /api/v1/resource_pool/resources/simple/by_type/{entity_kind}` 或 `POST /api/v1/resource_pool/resources/pick?entity_kind=...`
- `situation_pool` 做轻量展示：优先用 `GET /api/v1/situation_pool/resources/simple`
- `situation_pool` 做按类型投影：用 `GET/POST /api/v1/situation_pool/resources/simple/by_type/{situation_type}` 或 `POST /api/v1/situation_pool/resources/pick?situation_type=...`
- 不要把接口中附带的 `send_notification` 参数当作查询语义的一部分

## 12. 调用注意事项

- `/resources/query` 和 `/resources/search` 默认返回精简字段，不是完整对象。
- 如果某个 `task_type` 没有配置默认 simple 模板，`GET /simple` 系列不会返回该类型的数据。
- `query` 的 `mission_ids` 是"全部包含"语义，不是"任一命中"。
- `search` 是分词加分模型，不能把它当成精确条件查询。
- `pick` 的 `include` 模式必须提供 `paths` 或 `template`，`exclude` 模式必须提供 `paths`。
- `pick` 只按 `task_type` 筛选，不支持 `query` 中的多维度过滤条件。
- `resource_pool_refactor` 的 `simple` / `pick` 文档重点是字段投影，不包含 `task_pool` 那套 `query/search/retrieval` 语义。
- `resource_pool_refactor` 的 `by_type` 实际筛选字段是 `entity_kind`，不是 `task_type`。
- `resource_pool_refactor` 的默认 simple 模板是统一的设备白名单模板，不会像 `task_pool` 那样按不同类型切换不同默认模板。
- `situation_pool` 的筛选字段是 `situation_type`，不是 `task_type`，也不是 `entity_kind`。
- `situation_pool` 的 `simple` 没有 `get_latest_by_type` 路由。
- `situation_pool` 的默认模板是一次性包含所有目标类型字段的大集合，适合态势概览场景。

## 13. 三池对照速查表

| 维度 | task_pool | resource_pool_refactor | situation_pool |
| --- | --- | --- | --- |
| 默认端口 | `28801` | `28800` | `28802` |
| API 前缀 | `/api/v1/task_pool/resources` | `/api/v1/resource_pool/resources` | `/api/v1/situation_pool/resources` |
| 类型筛选字段 | `task_type` | `entity_kind` | `situation_type` |
| `simple` 有 `get_latest_by_type` | 是 | 是 | 否 |
| `pick` 路由 | `/pick/{task_type}` (路径参数) | `/pick` (查询参数 `entity_kind`) | `/pick` (查询参数 `situation_type`) |
| `simple` 默认模板 | 按 `task_type` 多套模板 | 统一设备白名单 | 一次性多目标类型大集合 |
| `simpleRequest` 请求体 | `TaskSimpleRequest` | `ResourceSimpleRequest` | `SituationSimpleRequest` |
| `pickRequest` 请求体 | `TaskPickRequest` | `ResourcePickRequest` | `SituationPickRequest` |
