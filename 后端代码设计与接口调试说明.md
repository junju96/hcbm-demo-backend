# ZK 指控后端服务 — 代码设计与接口调试说明

> 版本: 0.1.0 | 服务端口: 28600 | 技术栈: FastAPI + Pydantic + Uvicorn

---

## 一、项目概述

本后端服务为 **杀伤链（Kill Chain）管理模块** 提供 RESTful API 与 SSE 实时事件流支持。主要功能包括：

- 杀伤链的 CRUD 管理（创建、查询、更新、删除）
- 杀伤链条目（Entry）的增删与资源分配
- 候选装备资源的智能推荐与查询
- 行动方案的自动生成（由杀伤链映射为 PLAN）
- SSE 双流推送：Overview 摘要流 + Detail 详情流
- Task Pool 代理接口（资源查询、批量导入、状态获取）

当前阶段为 **Mock 后端**，所有数据存储在内存中（`TaskPool` 类模拟 Redis + SQLite），便于前端并行开发。

---

## 二、技术栈与依赖

| 依赖 | 版本 | 用途 |
|------|------|------|
| FastAPI | ≥0.110.0 | Web 框架，自动生成 OpenAPI 文档 |
| Uvicorn | ≥0.29.0 | ASGI 服务器，支持 WebSocket / SSE |
| Pydantic | ≥2.6.0 | 数据校验与序列化 |
| python-multipart | ≥0.0.9 | 表单数据解析 |

```bash
# 安装依赖
pip install -r requirements.txt

# 启动服务（开发模式，带热重载）
python3 -m uvicorn app.main:app --host 0.0.0.0 --port 28600 --reload

# 或直接用 main.py 入口
python3 app/main.py
```

---

## 三、目录结构

```
demo/backend/
├── app/
│   ├── __init__.py
│   ├── main.py                 # FastAPI 入口， lifespan/CORS/路由注册
│   ├── models/
│   │   ├── __init__.py
│   │   └── schemas.py          # Pydantic 模型：请求体 / 响应体 / 业务实体
│   ├── routers/
│   │   ├── __init__.py
│   │   ├── kill_chain.py       # 杀伤链核心 API（~660 行，主路由）
│   │   └── planning.py         # SSE 事件流端点（overview + detail）
│   ├── services/
│   │   ├── __init__.py
│   │   ├── task_pool.py        # 内存级数据存储（模拟 Redis + SQLite）
│   │   └── sse_manager.py      # SSE 连接管理与事件推送
│   └── data/
│       ├── __init__.py
│       └── mock_data.py        # 启动时预置的 Mock 数据
├── requirements.txt
└── 后端代码设计与接口调试说明.md   # 本文档
```

---

## 四、核心架构设计

### 4.1 模块职责

```
┌─────────────────────────────────────────────────────────────┐
│                        FastAPI App                           │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │ CORS        │  │ lifespan    │  │ 路由注册             │  │
│  │ Middleware  │  │ (mock预载)  │  │ /api/v1/*           │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
┌───────────────┐    ┌───────────────┐    ┌───────────────┐
│ kill_chain    │    │ planning      │    │ task_pool     │
│ router        │    │ router (SSE)  │    │ router (代理)  │
│ RESTful API   │    │ event-stream  │    │ 通用 CRUD     │
└───────┬───────┘    └───────┬───────┘    └───────┬───────┘
        │                    │                    │
        └────────────────────┼────────────────────┘
                             ▼
                   ┌─────────────────┐
                   │   TaskPool      │  ◄── 内存存储（替代 Redis/SQLite）
                   │  (单例)         │
                   └─────────────────┘
                             │
                             ▼
                   ┌─────────────────┐
                   │  SSEManager     │  ◄── 事件分发中心
                   │  (单例)         │      overview_queues + detail_queues
                   └─────────────────┘
```

### 4.2 数据流

**写入流（前端 → 后端）**
```
前端请求 → Router → Pydantic 校验 → TaskPool.patch/set → SSEManager 推送 → 前端接收更新
```

**读取流（前端 → 后端）**
```
前端请求 → Router → TaskPool.get/query → ApiResponse 包装 → JSON 返回
```

**SSE 实时流（后端 → 前端）**
```
TaskPool 数据变更 → SSEManager.push_* → asyncio.Queue → StreamingResponse (SSE)
```

### 4.3 单例管理

| 单例 | 文件 | 说明 |
|------|------|------|
| `task_pool` | `services/task_pool.py` | 全局内存存储，所有路由共享 |
| `sse_manager` | `services/sse_manager.py` | 全局 SSE 事件中心，所有路由共享 |

两者在 `main.py` 的 `lifespan` 中挂载到 `app.state`，支持通过 `request.app.state` 访问（当前直接全局导入使用）。

---

## 五、数据模型设计

### 5.1 核心实体关系

```
KillChain (杀伤链)
├── raw_entries[]      ──► KillChainEntry (phase=RAW, 候选阶段)
├── assigned_entries[] ──► KillChainEntry (phase=ASSIGNED, 已分配)
├── mapping_summary    ──► 映射统计信息
├── network            ──► 节点状态图 {nodes[], edges[]}
├── connections[]      ──► 关联 PLAN
└── state              ──► INIT / ACTIVE / DEACTIVATED / DELETED

KillChainEntry (杀伤链条目)
├── entry_id, entry_seq, operation
├── target_ids[]
├── executor_options[] ──► KillChainExecutorOption (候选装备列表)
├── selected_executor  ──► 最终选中的装备 ID
└── locked / is_valid

Plan (行动方案，由杀伤链映射生成)
├── teams[]            ──► Team (按装备分组)
├── targets[]          ──► TargetAbstract
├── stages[]           ──► Stage (执行阶段)
└── state              ──► DRAFT_EDITING / READY / EXECUTING
```

### 5.2 Pydantic 模型速查表

| 模型 | 用途 | 所在路由 |
|------|------|----------|
| `KillChainCreate` | POST /kill-chains 请求体 | kill_chain |
| `KillChainUpdate` | PATCH /kill-chains/{id} 请求体 | kill_chain |
| `KillChainEntryCreate` | POST /kill-chains/{id}/entries | kill_chain |
| `AutoAllocateRequest` | POST .../auto-allocate | kill_chain |
| `ResourceAllocate` | POST .../allocate | kill_chain |
| `GeneratePlanRequest` | POST .../generate-plan | kill_chain |
| `ResourceQuery` | POST /kill-chains/resources/query | kill_chain |
| `PlanCreate` | 内部生成 Plan 时构建 | kill_chain |
| `ApiResponse` | 统一响应包装（code/message/data） | 全局 |

---

## 六、API 接口清单

### 6.1 基础接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/health` | 健康检查 |

### 6.2 杀伤链接口（前缀 `/api/v1`）

#### CRUD

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/kill-chains` | 创建杀伤链 |
| GET | `/kill-chains/{id}` | 获取杀伤链详情 |
| PATCH | `/kill-chains/{id}` | 更新杀伤链 |
| DELETE | `/kill-chains/{id}` | 删除杀伤链 |
| POST | `/kill-chains/{id}/forward` | 转发杀伤链 |

#### Entry 管理

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/kill-chains/{id}/entries` | 增加条目 |
| DELETE | `/kill-chains/{id}/entries/{entry_id}` | 删除条目 |

#### 资源分配

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/kill-chains/resources/query` | 查询候选装备 |
| POST | `/kill-chains/{id}/entries/{entry_id}/auto-allocate` | 自动分配 |
| POST | `/kill-chains/{id}/entries/{entry_id}/allocate` | 人工分配 |

#### 方案生成与状态

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/kill-chains/{id}/generate-plan` | 生成行动方案 |
| POST | `/kill-chains/{id}/activate` | 激活杀伤链 |
| POST | `/kill-chains/{id}/deactivate` | 静默杀伤链 |
| POST | `/decision/kill-chain-remapping` | 杀伤链重映射 |

### 6.3 Task Pool 代理接口（前缀 `/api/v1`）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/task_pool/resources/query` | 按条件查询资源 |
| GET | `/task_pool/resources/{id}` | 获取资源详情 |
| PATCH | `/task_pool/resources/{id}` | 增量更新资源 |
| POST | `/task_pool/ingestion/import` | 批量导入资源 |
| POST | `/task_pool/ingestion/commit` | 提交 pending 数据 |

### 6.4 资源池代理接口（前缀 `/api/v1`）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/resources/query` | 查询装备资源 |
| GET | `/resources/{id}/state` | 获取装备实时状态 |
| POST | `/resources/batch/state` | 批量获取装备状态 |

### 6.5 SSE 事件流接口（前缀 `/api/v1`）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/planning/events/overview?client_id=xxx&scope=planning_home` | 摘要 SSE 流 |
| GET | `/planning/events/detail?client_id=xxx&resource_type=kill_chain&resource_id=xxx` | 详情 SSE 流 |

---

## 七、接口调试示例

> 以下示例假设服务运行在 `http://localhost:28600`

### 7.1 健康检查

```bash
curl -s http://localhost:28600/health | python3 -m json.tool
```

**预期响应**
```json
{
    "status": "ok",
    "service": "zk-backend"
}
```

### 7.2 获取杀伤链详情

```bash
curl -s http://localhost:28600/api/v1/kill-chains/kc-ssl-demo-001 | python3 -m json.tool
```

**预期响应**（节选）
```json
{
    "code": 200,
    "message": "success",
    "data": {
        "resource_id": "kill_chain:kc-ssl-demo-001",
        "task_type": "KILL_CHAIN",
        "kill_chain_id": "kc-ssl-demo-001",
        "title": "目标1/目标2-杀伤链构建示例",
        "state": "ACTIVE",
        "raw_entries": [...],
        "assigned_entries": [...],
        "mapping_summary": {...},
        "network": {"nodes": [...], "edges": [...]}
    }
}
```

### 7.3 创建杀伤链

```bash
curl -s -X POST http://localhost:28600/api/v1/kill-chains \
  -H "Content-Type: application/json" \
  -d '{
    "title": "新建杀伤链-测试",
    "description": "通过API创建的测试杀伤链",
    "targets": [
      {"target_id": "target-3", "target_name": "目标3"}
    ]
  }' | python3 -m json.tool
```

**预期响应**
```json
{
    "code": 200,
    "message": "success",
    "data": {
        "kill_chain_id": "kc-a1b2c3d4",
        "resource_id": "kill_chain:kc-a1b2c3d4",
        "status": "created"
    }
}
```

### 7.4 增加杀伤链条目

```bash
curl -s -X POST http://localhost:28600/api/v1/kill-chains/kc-ssl-demo-001/entries \
  -H "Content-Type: application/json" \
  -d '{
    "entry_seq": 7,
    "target_ids": ["target:target-2"],
    "operation": "打击",
    "notes": "新增打击条目"
  }' | python3 -m json.tool
```

### 7.5 查询候选装备

```bash
# 按作战动作查询
curl -s -X POST http://localhost:28600/api/v1/kill-chains/resources/query \
  -H "Content-Type: application/json" \
  -d '{"operation": "侦察", "limit": 10}' | python3 -m json.tool

# 带关键字过滤
curl -s -X POST http://localhost:28600/api/v1/kill-chains/resources/query \
  -H "Content-Type: application/json" \
  -d '{"operation": "打击", "keyword": "车B", "limit": 5}' | python3 -m json.tool
```

**预期响应**
```json
{
    "code": 200,
    "message": "success",
    "data": {
        "items": [
            {
                "executor_id": "equipment:vehicle-A",
                "resource_name": "无人车A",
                "resource_type": "UGV",
                "allocation_count": 0,
                "locked": false,
                "note": "合法；无人车A具备侦察能力"
            },
            {
                "executor_id": "equipment:vehicle-B",
                "resource_name": "无人车B",
                "resource_type": "UGV",
                "allocation_count": 0,
                "locked": true,
                "note": "不推荐；无人车B侦察能力弱"
            }
        ],
        "total": 2
    }
}
```

### 7.6 自动分配资源

```bash
curl -s -X POST http://localhost:28600/api/v1/kill-chains/kc-ssl-demo-001/entries/raw-002/auto-allocate \
  -H "Content-Type: application/json" \
  -d '{
    "operation": "定位",
    "target_ids": ["target:target-1", "target:target-2"],
    "constraints": {}
  }' | python3 -m json.tool
```

**预期响应**
```json
{
    "code": 200,
    "message": "success",
    "data": {
        "job_id": "auto-alloc-xxxx",
        "kill_chain_id": "kc-ssl-demo-001",
        "entry_id": "raw-002",
        "selected_executor": "equipment:vehicle-A",
        "status": "SUCCEEDED"
    }
}
```

### 7.7 人工分配资源

```bash
curl -s -X POST http://localhost:28600/api/v1/kill-chains/kc-ssl-demo-001/entries/raw-003/allocate \
  -H "Content-Type: application/json" \
  -d '{
    "selected_executor": "equipment:vehicle-B",
    "allocation_type": "manual",
    "reason": "测试人工分配"
  }' | python3 -m json.tool
```

### 7.8 生成行动方案

```bash
curl -s -X POST http://localhost:28600/api/v1/kill-chains/kc-ssl-demo-001/generate-plan \
  -H "Content-Type: application/json" \
  -d '{
    "selected_entry_ids": ["assigned-001", "assigned-004", "assigned-005"],
    "plan_config": {
      "title": "测试方案",
      "description": "人工测试生成的方案"
    }
  }' | python3 -m json.tool
```

**预期响应**
```json
{
    "code": 200,
    "message": "success",
    "data": {
        "plan_id": "plan:plan-xxxx",
        "kill_chain_id": "kc-ssl-demo-001",
        "status": "SUCCEEDED",
        "message": "方案生成完成"
    }
}
```

### 7.9 激活 / 静默杀伤链

```bash
# 激活
curl -s -X POST http://localhost:28600/api/v1/kill-chains/kc-ssl-demo-001/activate | python3 -m json.tool

# 静默
curl -s -X POST http://localhost:28600/api/v1/kill-chains/kc-ssl-demo-001/deactivate | python3 -m json.tool
```

### 7.10 Task Pool 资源查询

```bash
# 查询所有 KILL_CHAIN
curl -s -X POST http://localhost:28600/api/v1/task_pool/resources/query \
  -H "Content-Type: application/json" \
  -d '{"task_type": "KILL_CHAIN", "limit": 10}' | python3 -m json.tool

# 查询所有 EQUIPMENT
curl -s -X POST http://localhost:28600/api/v1/task_pool/resources/query \
  -H "Content-Type: application/json" \
  -d '{"task_type": "EQUIPMENT", "limit": 10}' | python3 -m json.tool
```

### 7.11 装备实时状态

```bash
# 单个状态
curl -s http://localhost:28600/api/v1/resources/equipment:vehicle-A/state | python3 -m json.tool

# 批量状态
curl -s -X POST http://localhost:28600/api/v1/resources/batch/state \
  -H "Content-Type: application/json" \
  -d '{"resource_ids": ["equipment:vehicle-A", "equipment:vehicle-B"]}' | python3 -m json.tool
```

### 7.12 SSE 事件流测试

```bash
# Overview 流（连接后持续接收事件）
curl -N http://localhost:28600/api/v1/planning/events/overview?client_id=client-1\&scope=planning_home

# Detail 流（连接后持续接收事件）
curl -N "http://localhost:28600/api/v1/planning/events/detail?client_id=client-1&resource_type=kill_chain&resource_id=kill_chain:kc-ssl-demo-001"
```

**SSE 输出示例**
```
event: planning.overview.init
data: {"commands": [], "missions": [], "plans": [...], "kill_chains": [...]}

event: planning.overview.changed
data: {"resource_type": "KILL_CHAIN", "resource_id": "kill_chain:kc-ssl-demo-001", "action": "PATCH", "summary": {...}}

event: system.heartbeat
data: {"timestamp": "2026-05-26T01:42:40.600281+00:00"}
```

---

## 八、Mock 数据说明

启动时自动预置以下数据：

| 数据类型 | 数量 | 关键 ID |
|----------|------|---------|
| 态势目标 (TARGET) | 2 | `target:target-1`, `target:target-2` |
| 装备资源 (EQUIPMENT) | 2 | `equipment:vehicle-A`, `equipment:vehicle-B` |
| 杀伤链 (KILL_CHAIN) | 1 | `kill_chain:kc-ssl-demo-001` |
| 行动方案 (PLAN) | 1 | `plan:plan-ssl-demo-001` |

### 装备能力对照

| 装备 | 侦察能力 | 打击能力 | 在线状态 |
|------|----------|----------|----------|
| 无人车A (vehicle-A) | recon_range_km=3 ✅ | 无 ❌ | ONLINE |
| 无人车B (vehicle-B) | 无 ❌ | strike_range_km=4 ✅ | ONLINE |

### 自动分配策略

- **侦察/定位/跟踪/评估**：优先推荐具备 `recon_range_km` 的装备
- **瞄准/引导/打击**：优先推荐具备 `strike_range_km` 的装备
- 自动分配时选择第一个未锁定的候选

---

## 九、关键设计决策

### 9.1 统一响应格式

所有 API 返回统一的 `ApiResponse` 结构：

```json
{
    "code": 200,
    "message": "success",
    "data": { ... }
}
```

错误时：
```json
{
    "code": 400,
    "message": "无可用的自动分配候选",
    "data": null
}
```

### 9.2 SSE 双通道设计

| 通道 | 作用域 | 事件类型 | 典型事件 |
|------|--------|----------|----------|
| Overview | planning_home / command_decomposition | 卡片摘要变更 | `planning.overview.init`, `planning.overview.changed` |
| Detail | resource_type:resource_id | 全量详情变更 | `planning.detail.init`, `planning.kill_chain.entries_changed` |

**心跳机制**：每 30 秒自动发送 `system.heartbeat`，防止连接被代理断开。

### 9.3 内存存储 vs 持久化

当前 `TaskPool` 为纯内存实现，支持：
- 基础 CRUD（get/set/patch/delete）
- 条件查询（按 task_type/state/keyword/parent）
- 全文搜索
- 生命周期管理（软删除）
- 批量导入
- Commit 模拟（标记 pending 状态）

**后续迁移路径**：将 `_store` 替换为 Redis client，`commit()` 替换为 SQLite 写入即可。

### 9.4 ID 规范

| 前缀 | 用途 | 示例 |
|------|------|------|
| `kill_chain:` | 杀伤链资源 ID | `kill_chain:kc-ssl-demo-001` |
| `plan:` | 方案资源 ID | `plan:plan-ssl-demo-001` |
| `equipment:` | 装备资源 ID | `equipment:vehicle-A` |
| `target:` | 目标资源 ID | `target:target-1` |
| `kc-` | 杀伤链业务 ID | `kc-a1b2c3d4` |
| `entry-` | 条目 ID | `entry-a1b2c3d4` |
| `assigned-` | 已分配条目 ID | `assigned-a1b2c3d4` |
| `plan-` | 方案业务 ID | `plan-a1b2c3d4` |

---

## 十、OpenAPI 文档

服务启动后自动生成交互式 API 文档：

- Swagger UI: `http://localhost:28600/docs`
- ReDoc: `http://localhost:28600/redoc`
- OpenAPI JSON: `http://localhost:28600/openapi.json`

---

## 十一、常见问题

**Q: 启动时报 `ModuleNotFoundError: No module named 'app'`**

A: 确保在 `demo/backend/` 目录下启动，且使用 `python3 -m uvicorn app.main:app`。

**Q: 前端调用 API 报 CORS 错误**

A: 已在 `main.py` 中配置 `allow_origins=["*"]`，确保前端请求 URL 与后端域名+端口正确对应。

**Q: SSE 连接断开后不自动重连**

A: 前端需实现 `EventSource.onerror` 重连逻辑（指数退避）。后端心跳每 30 秒一次。

**Q: 数据重启后丢失**

A: 当前为内存存储，重启后数据重置。后续接入 Redis + SQLite 后持久化。
